package proto

// Define some kind of message encoding
// since we're dealing with mainly a cryptographic
// protocol prefer proper serialization to JSON based
// encodings to avoid byte-level encoding errors.
